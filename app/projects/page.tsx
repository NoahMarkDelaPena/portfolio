import React, { FC } from "react";

interface ProjectsProps {}

const page: FC<ProjectsProps> = () => {
  return (
    <main className="flex justify-center items-center min-h-screen">page</main>
  );
};

export default page;
